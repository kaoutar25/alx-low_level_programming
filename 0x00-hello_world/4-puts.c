#include <stdio.h>
/**
 * main - A program that prints a line with puts function
 * Return: 0 (Success)
 */
int main(void)
{
puts("\"programming is like buildung a multilingual puzzle");
return (0);
}
